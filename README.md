# gguf-serving-cache-llamacpp
Serving a fine-tuned GGUF model with llama.cpp behind a SQL response cache, with GPU-offload benchmarking and a demo of how deterministic caching can lock in a wrong answer.
